nodes:
  - address: 10.10.10.70
    user: root
    role: [controlplane, worker, etcd]
  - address: 10.10.10.71
    user: root
    role: [controlplane, worker, etcd]
  - address: 10.10.10.72
    user: root
    role: [controlplane, worker, etcd]

services:
  etcd:
    snapshot: true
    creation: 6h
    retention: 24

ingress:
  provider: nginx
  options:
    use-forwarded-headers: "true"
